<div class="p-10 flex flex-col gap-7">
  <%= render 'users/user_tile', user: @user %>
  <div class="p-4 flex flex-col gap-4 border-black">
    <p class="text-base font-bold">Bio</p>
    <p><%= @user.bio %></p>
  </div>
   <ul>
    <% @all_posts.each do |post| %>
      <li>
        <%= link_to user_post_path(@user, post) do %>
          <%= render 'posts/post_tile', post: post %>
        <% end %>
      </li>
    <% end %>
  </ul>
  <div class="flex justify-center">
    <%= link_to user_posts_path(@user) do %>
      <button class="border-black h-10 w-28 cursor-pointer">See all posts</button>
    <% end %>
  </div>
</div>